#include "binance.h"

using namespace binance;
using namespace std;

binanceError_t binance::Market::get24hr(string& result, const string& url)
{
	return binanceSuccess;
}

binanceError_t binance::Market::getAggTrades(string& result, const string& url)
{
	return binanceSuccess;
}

binanceError_t binance::Market::getAllBookTickers(string& result)
{
	return binanceSuccess;
}

binanceError_t binance::Market::getAllPrices(string& result)
{
	return binanceSuccess;
}

binanceError_t binance::Market::getDepth(string& result, const string& url)
{
	return binanceSuccess;
}

binanceError_t binance::Market::getKlines(string& result, const string& url)
{
	return binanceSuccess;
}

binanceError_t binance::Market::getTrades(string& result, const string& url)
{
	return binanceSuccess;
}

